name: aims_data_platform
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip>=23.0
  - pip:
      - -r requirements.txt

# Optional: Specify conda packages for better performance
  - pandas>=2.0.0
  - numpy>=1.24.0
  - sqlalchemy>=2.0.0
